name: release-template

on:
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Verify
        run: |
          cargo fmt --all --check
          cargo clippy --all-targets --all-features -- -D warnings
          cargo test -- --skip live_

      - name: Publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: cargo publish

# Suggested activation:
# 1) copy to .github/workflows/release-tag.yml
# 2) replace `on.workflow_dispatch` with:
#    on:
#      push:
#        tags:
#          - 'v*.*.*'
# 3) add `CARGO_REGISTRY_TOKEN` secret
